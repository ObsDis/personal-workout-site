.DS_Store
node_modules/
*.log
.vscode/
.idea/
*.bak
*.backup
# Capacitor native projects
# iOS project IS tracked now so we have version history (incl. Pods for reproducible restores).
# To stop tracking the heavy Pods instead, uncomment the next line:
# ios/App/Pods/
android/
ios/App/build/
ios/App/DerivedData/
ios/**/xcuserdata/
*.xcuserstate
*.xcscmblueprint
.netlify/
# secrets — never commit
.env
.env.*
secrets.txt
keys.txt
*.secret
*.secrets
*.pat
.api-keys
.api-keys.*

# secrets / Apple API keys — never commit (public repo)
Apple/
*.p8
# internal working notes — keep private
PROJECT_HISTORY.md
CLAUDE_CONTEXT.md
SUBMIT_CHECKLIST.md
BARBELLMIND_MCP_README.md
*.pre-chat-backup

# iOS web bundle (local cap-copy input; app is served publicly from /app/)
www/
